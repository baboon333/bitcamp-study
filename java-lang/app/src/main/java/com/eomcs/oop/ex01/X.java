package com.eomcs.oop.ex01;

// 패키지 멤버 클래스  (class x 처럼 이렇게 독립적인 클래스)
// - 이 경우처럼 가능한 한 파일에 한 개의 클래스를 정의하라!
// - 그래야 클래스를 찾기 쉽다.
// - 즉 유지보수하기 쉽다.
// 패키지 멤버는 static을 못붙인다!

// 비공개 패키지 멤버 클래스  (public이 안붙었기 때문에)
// - 같은 패키지에 소속된 클래스만이 이 클래스를 사용할 수 있다.
class X {

}

